# Author-supplied validation record: abc123

Scope: whole stack at tip abc123, not a separate run of the base PR.

Monday: `npm test` in the sandbox reported two subprocess failures.
Tuesday: unrestricted `npm test` passed all 852 tests; `npm run typecheck`
and `npm run lint` also passed. This rerun resolved the subprocess failures.

Unit tests stub the remote API. A live Actions campaign has never been run.
These are author-reported results, not observations by the prose editor.
